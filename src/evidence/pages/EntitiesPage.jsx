import PlaceholderPage from './PlaceholderPage';

export default function EntitiesPage() {
  return (
    <PlaceholderPage
      title="Entities"
      description="Entity review is staged until entity, alias, and merge-decision routes are available."
    />
  );
}
