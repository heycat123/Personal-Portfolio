import PlaceholderPage from './PlaceholderPage';

export default function QueryPage() {
  return (
    <PlaceholderPage
      title="Query"
      description="Evidence answers stay disabled until the query API returns citations, verified facts, retrieval trace, and cost metadata."
    />
  );
}
